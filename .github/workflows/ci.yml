name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:  # Allow manual triggers

jobs:
  lint:
    name: Lint with Ruff
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.13'
          cache: 'pip'

      - name: Install dependencies
        run: |
          # Install dev dependencies (includes ruff)
          if [ -f requirements-dev.txt ]; then
            pip install -r requirements-dev.txt
          fi

      - name: Run ruff check
        run: |
          # Only run if Python files exist
          if find . -name "*.py" -type f | grep -q .; then
            ruff check .
          else
            echo "No Python files found, skipping ruff check"
          fi

      - name: Check ruff formatting
        run: |
          # Only run if Python files exist
          if find . -name "*.py" -type f | grep -q .; then
            ruff format --check .
          else
            echo "No Python files found, skipping ruff format check"
          fi

  typecheck:
    name: Type Check with mypy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.13'
          cache: 'pip'

      - name: Install dependencies
        run: |
          # Install project dependencies
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          fi
          # Install dev dependencies
          if [ -f requirements-dev.txt ]; then
            pip install -r requirements-dev.txt
          fi

      - name: Run mypy
        run: |
          # Only run if Python files exist
          if find src/ -name "*.py" -type f | grep -q .; then
            mypy src/ --config-file=pyproject.toml
          else
            echo "No Python files found in src/, skipping mypy"
          fi

  test:
    name: Test with pytest
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.13']

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # Install project dependencies
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          fi
          # Install dev dependencies
          if [ -f requirements-dev.txt ]; then
            pip install -r requirements-dev.txt
          fi
          # Install package in editable mode if pyproject.toml exists
          if [ -f pyproject.toml ]; then
            pip install -e .
          fi

      - name: Run tests with coverage
        run: |
          # Check if tests exist
          if find tests/ -name "test_*.py" -type f | grep -q .; then
            pytest tests/ \
              --cov=src \
              --cov-report=term-missing \
              --cov-report=xml \
              --cov-report=html \
              --html=test-report.html \
              --self-contained-html \
              -v
          else
            echo "No test files found, skipping pytest"
            echo "This is expected for a template repository"
          fi

      - name: Upload coverage to artifacts
        if: matrix.python-version == '3.13' && hashFiles('htmlcov/**') != ''
        uses: actions/upload-artifact@v6
        with:
          name: coverage-report
          path: htmlcov/
          retention-days: 30

      - name: Upload test report to artifacts
        if: matrix.python-version == '3.13' && hashFiles('test-report.html') != ''
        uses: actions/upload-artifact@v6
        with:
          name: test-report
          path: test-report.html
          retention-days: 30

      - name: Coverage summary
        if: matrix.python-version == '3.13' && hashFiles('.coverage') != ''
        run: |
          echo "## Test Coverage Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          coverage report >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
